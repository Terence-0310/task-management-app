// Optional stub to avoid installing 'web-vitals'.
export type ReportHandler = (metric: unknown) => void;

const reportWebVitals = (_?: ReportHandler) => {
  // no-op
};

export default reportWebVitals;
